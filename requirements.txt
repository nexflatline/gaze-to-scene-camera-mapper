# Core dependencies for eye-tracking gaze mapper
# Install with: pip install -r requirements.txt
#
# Note: tkinter is required but ships with Python by default on Windows and macOS.
# On Linux (Ubuntu/Debian) install it separately: sudo apt-get install python3-tk

opencv-python>=4.5
numpy>=1.21
pandas>=1.3
scikit-learn>=0.24
Pillow>=8.0
